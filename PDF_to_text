import fitz  # PyMuPDF
import pytesseract
from PIL import Image
import os
import io

# Укажите путь к tesseract если он не добавлен в PATH
# pytesseract.pytesseract.tesseract_cmd = r'C:\Program Files\Tesseract-OCR\tesseract.exe'

def pdf_to_text(input_folder, output_folder):
    # Создаем папку для результатов если её нет
    if not os.path.exists(output_folder):
        os.makedirs(output_folder)

    # Проходим по всем файлам в папке
    for filename in os.listdir(input_folder):
        if filename.lower().endswith('.pdf'):
            pdf_path = os.path.join(input_folder, filename)
            text = extract_text_from_pdf(pdf_path)
            
            # Сохраняем результат в txt файл
            output_filename = os.path.splitext(filename)[0] + '.txt'
            output_path = os.path.join(output_folder, output_filename)
            
            with open(output_path, 'w', encoding='utf-8') as f:
                f.write(text)
            print(f"Обработан: {filename}")

def extract_text_from_pdf(pdf_path):
    doc = fitz.open(pdf_path)
    page = doc.load_page(0)  # Первая страница
    
    # Получаем изображение страницы с повышенным DPI
    mat = fitz.Matrix(300/72, 300/72)  # 300 DPI
    pix = page.get_pixmap(matrix=mat)
    img_data = pix.tobytes('ppm')
    
    # Конвертируем в изображение для OCR
    image = Image.open(io.BytesIO(img_data))
    
    # Применяем OCR с указанием языков
    text = pytesseract.image_to_string(
        image,
        lang='rus+eng',  # Русский и английский
        config='--psm 3 --oem 3'
    )
    
    doc.close()
    return text

if __name__ == "__main__":
    input_folder = "путь/к/папке/с/pdf"
    output_folder = "путь/к/папке/результатов"
    pdf_to_text(input_folder, output_folder)